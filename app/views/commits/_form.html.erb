<%= form_with(model: commit, local: true) do |form| %>
  <% if commit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commit.errors.count, "error") %> prohibited this commit from being saved:</h2>

      <ul>
      <% commit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :commit_content %>
  </div>

  <div class="field">
    <%= form.label :micropost_id %>
    <%= form.text_field :micropost_id, id: :commit_micropost_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
