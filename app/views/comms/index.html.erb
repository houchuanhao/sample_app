<p id="notice"><%= notice %></p>

<h1>Comms</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th>Micropost</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comms.each do |comm| %>
      <tr>
        <td><%= comm.content %></td>
        <td><%= comm.user %></td>
        <td><%= comm.micropost %></td>
        <td><%= link_to 'Show', comm %></td>
        <td><%= link_to 'Edit', edit_comm_path(comm) %></td>
        <td><%= link_to 'Destroy', comm, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Comm', new_comm_path %>
