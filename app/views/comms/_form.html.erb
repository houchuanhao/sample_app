<%= form_with(model: comm, local: true) do |form| %>
  <% if comm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comm.errors.count, "error") %> prohibited this comm from being saved:</h2>

      <ul>
      <% comm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :comm_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :comm_user_id %>
  </div>

  <div class="field">
    <%= form.label :micropost_id %>
    <%= form.text_field :micropost_id, id: :comm_micropost_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
